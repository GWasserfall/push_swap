#include "push_swap.h"

// Recursive sort
// void ft_recursivesort(t_stack **s) {
//     if (!ft_isempty(s)) {
//         pa(s);
//         int x = ft_top();
//         ft_recursivesort(s);
//         insert(s, x);
//     }
// }

// void insert(t_stack s, int x) {
//     if (!IsEmpty(s)) {  
//         int y = Top(s);
//         if (x < y) {
//             Pop(s);
//             insert(s, x);
//             Push(s, y);
//         } else {
//             Push(s, x);
//         }
//     } else {
//         Push(s, x); 
//     }
// }
